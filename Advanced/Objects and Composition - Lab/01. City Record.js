function cityRecords(name,population,treasury) {

    let person = {
        name: name,
        population: population,
        treasury: treasury
    }

    console.log(person);
}

cityRecords("Tortuga", 7000, 15000);
